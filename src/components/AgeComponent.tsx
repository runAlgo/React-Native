import { View, Text } from 'react-native'
import React from 'react'
import { ss } from '../utils/style'
const AgeComponent = () => {
  return (
    <View style={ss.shadowStyle}>
      <Text>AgeComponent</Text>
    </View>
  )
}

export default AgeComponent