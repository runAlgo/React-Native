import { View, Text } from 'react-native'
import React from 'react'
import { ss } from '../utils/style'

const HandleComponent = () => {
  return (
    <View style={ss.shadowStyle}>
      <Text>HandleComponent</Text>
    </View>
  )
}

export default HandleComponent